{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev",
    "check": "astro check",
    "test": "vitest run",
    "test:watch": "vitest",
    "astro": "astro",
    "db:init:local": "wrangler d1 execute jojojiji-comments --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute jojojiji-comments --remote --file=./schema.sql"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.17.1",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.0"
  }
}
